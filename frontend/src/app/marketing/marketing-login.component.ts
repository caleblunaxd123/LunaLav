import { Component, inject, signal } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { MarketingAuthService } from './marketing-auth.service';
@Component({standalone:true,imports:[FormsModule],template:`<main class="login"><section><div class="brand">◒ <b>LunaLav</b><span>Marketing</span></div><h1>Centro comercial</h1><p>Organiza oportunidades, seguimientos y demos desde un solo lugar.</p><form (ngSubmit)="entrar()"><label>Usuario<input [(ngModel)]="usuario" name="usuario" autocomplete="username" required autofocus></label><label>Contraseña<input [(ngModel)]="password" name="password" type="password" autocomplete="current-password" required></label>@if(error()) { <small>{{error()}}</small> }<button [disabled]="cargando()">{{cargando()?'Ingresando…':'Ingresar a Marketing'}}</button></form><footer>Acceso interno de LunaLav</footer></section></main>`,styles:[`.login{min-height:100vh;display:grid;place-items:center;background:radial-gradient(circle at 80% 10%,#c7ecff,transparent 35%),#f6f9fc;color:#123257}.login section{width:min(420px,calc(100% - 36px));background:#fff;padding:44px;border:1px solid #dfe8f0;border-radius:22px;box-shadow:0 24px 60px #1b4b7520}.brand{font-size:21px;color:#1999df}.brand span{font-size:12px;font-weight:800;text-transform:uppercase;letter-spacing:1.2px;margin-left:9px;color:#5f7892}h1{font-size:31px;margin:28px 0 8px}p{color:#61778d;line-height:1.55}form{display:grid;gap:16px;margin-top:27px}label{display:grid;gap:7px;font-size:13px;font-weight:800}input{border:1px solid #cbd8e5;border-radius:9px;padding:12px;font-size:15px}button{background:#07599e;color:white;border:0;padding:13px;border-radius:9px;font-size:15px;font-weight:800;cursor:pointer}button:disabled{opacity:.6}small{color:#c13a3a}footer{margin-top:26px;font-size:12px;color:#789;text-align:center}`]}) export class MarketingLoginComponent { private auth=inject(MarketingAuthService);private router=inject(Router);usuario='';password='';cargando=signal(false);error=signal('');entrar(){if(this.cargando())return;this.cargando.set(true);this.error.set('');this.auth.login(this.usuario.trim(),this.password).subscribe({next:()=>this.router.navigate(['/marketing']),error:e=>{this.error.set(e.error?.mensaje??'No se pudo iniciar sesión.');this.cargando.set(false)}});}}
